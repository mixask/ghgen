{
  "name": "ghgen-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:remote": "wrangler d1 execute greedyhudzell-keys --remote --file=./schema.sql",
    "migrate:local": "wrangler d1 execute greedyhudzell-keys --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
